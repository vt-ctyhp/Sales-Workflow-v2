const Tracker = Object.freeze({
  appendLog: function(rootApptId, entry) {
    return phaseNotImplemented_('Tracker.appendLog');
  },
  getEntries: function(rootApptId) {
    return phaseNotImplemented_('Tracker.getEntries');
  },
});
